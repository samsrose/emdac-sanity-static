2025-12-03 - Fix Sanity Studio Browser Compatibility and Configuration Issues

User Prompt 1: "Uncaught error: Module 'fs' has been externalized for browser compatibility. Cannot access 'fs.readFileSync' in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details."

Restructured: "Fix the Node.js module usage in Sanity Studio configuration that causes browser compatibility errors. Remove fs.readFileSync and other Node.js-specific modules from client-side code bundled by Vite. Switch to Vite's native environment variable handling using import.meta.env."

User Prompt 2: "Uncaught error: Workflow plugin: Missing 'schemaTypes' in config"

Restructured: "Configure the @multidots/sanity-plugin-workflow-manager plugin with required schemaTypes array. Identify appropriate document types from the schema configuration that should be managed by the workflow system."

User Prompt 3: "getting error: 'Configuration must contain `projectId`'"

Restructured: "Fix Sanity Studio v3 environment variable loading issue where projectId is undefined. Transform NEXT_PUBLIC_SANITY_* environment variables to SANITY_STUDIO_* prefix format that Vite-based Sanity Studio v3 expects. Update the copyEnv.js script to perform this transformation and ensure import.meta.env can access the required configuration values."

2026-05-02 - SOLID rebuild

User Prompt 4: "inspect project for SOLID practices"

Restructured: "Audit the codebase for adherence to SOLID principles, identifying violations across SRP, OCP, LSP, ISP, and DIP. Surface concrete examples (overloaded modules, copy-paste components, leaky abstractions, untyped boundaries) and prioritise findings by impact."

User Prompt 5: "Rebuild with SOLID principles for efficiency"

Restructured: "Plan and execute a refactor that addresses the SOLID audit findings. Confirm scope (full App Router + TypeScript rewrite vs incremental), delivery model (single PR vs split), and any out-of-scope choices (e.g., keep Sanity schemas as-is) before implementation."

User Prompt 6: "update the codebase to follow better principles"

Restructured: "Execute the agreed-upon SOLID rebuild on the current branch: critical safety fixes first, then the TypeScript + repository foundation, generic UI components replacing the duplicated *Item files, full App Router migration with typed routes and route handlers, and removal of the superseded /pages tree. Verify with `pnpm run build`."

User Prompt 7: "optimize code for performance"

Restructured: "Apply targeted performance optimizations on top of the rebuilt App Router codebase: shrink the client JavaScript bundle (split server vs client components, gate heavy integrations behind env flags, enable package import tree-shaking), correct responsive image sizing, restore Sanity CDN usage for server-side reads, add preconnect hints for the image CDN, and remove redundant work. Verify with `pnpm run build` and ensure no behavioural regressions."

User Prompt 8: "refactor components for best data efficiency in getting data from sanity"

Restructured: "Refactor the App Router pages and Sanity repository layer to minimise round trips and payload size: replace per-category Promise.all fan-outs with single composite GROQ object projections, add a lean projection for post metadata generation, and configure the public Sanity client with perspective:published so drafts are filtered server-side. Keep both composite and granular repository methods. Preserve current ISR behaviour. Verify with pnpm run build."
